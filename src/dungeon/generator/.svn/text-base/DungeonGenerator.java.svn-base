package dungeon.generator;

import dungeon.Cells;

/**
 * Common interface for dungeon generation
 *
 * @author Blind
 */
public interface DungeonGenerator {
    Cells generate(int row, int column);

    /**
     * Contains map generator id.
     *
     * @return id of map generator.
     */
    String getId();
}
